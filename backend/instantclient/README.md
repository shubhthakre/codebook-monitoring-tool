Oracle Instant Client is selected automatically for this OS.

Layout (binaries are gitignored):

  backend/instantclient-basic-windows.../instantclient_23_26   Windows (oci.dll)
  backend/instantclient_23_26                                  Linux (libclntsh.so*)

Optional aliases:

  backend/instantclient/windows
  backend/instantclient/linux

Leave ORACLE_CLIENT_LIB_DIR empty unless you need to override the path.
